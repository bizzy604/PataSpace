generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Replace this scaffold with the full schema from:
// Docs/02_Database_Schema.md
